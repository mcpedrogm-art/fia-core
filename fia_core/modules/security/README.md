# Security module

Lightweight security prompts for the current TASK: secrets, authentication,
authorization, input boundaries, dependencies, logging and deployment exposure.

Use it when the task touches users, data, credentials, external services or
deployment. It is guidance plus optional checks; Core does not require it.
