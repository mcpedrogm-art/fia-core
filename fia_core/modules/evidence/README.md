# Evidence module

Adds a human-readable index that explains why a TEST proves a condition and where
the supporting artifact lives. Core already stores hashed TEST output; this module
adds context, not another mandatory state machine.
