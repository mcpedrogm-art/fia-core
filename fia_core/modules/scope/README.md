# Scope module

Optional change-scope checks. A TASK can declare allowed paths or boundaries and a
Git adapter can report changes outside them. It is off by default because not every
project uses Git or needs path enforcement.
