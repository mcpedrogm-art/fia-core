# Provenance module

Optional trust metadata for CI artifacts: producer, run URL, commit, platform and
digest. Use it when local test output is not enough. It is separate from Core's
local integrity hashes.
